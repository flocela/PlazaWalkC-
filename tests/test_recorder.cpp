#include "catch.hpp"
#include "../src/Recorder.h"

using namespace std;
/*
TEST_CASE("incoming boxes are recorded")

    Recorder recorder{};
    recorder.change(Position{5, 5}, BoardNote{0, 2});
    recorder.change(Position{6, 6}, BoardNote{0, 2});
    recorder.change(Position{7, 7}, BoardNote{0, 2});

    unordered_map<Position, int> actualTypesPerPositions = recorder.getPositions();

    unordered_map<Position, int> expectedTypesPerPositions{};
    expectedTypesPerPositions{
*/
