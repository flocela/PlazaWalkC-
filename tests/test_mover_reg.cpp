#include "catch.hpp"
/*#include "../src/Mover_Reg.cpp"

TEST_CASE("Moving a box on a board, adds the correct BoxNotes to the box and the correct BoardNotes to the board, in the correct order.")
{
    Board board{10, 10};
    Box box{0, 10, 10};

    Mover_Reg mover{};

    Position positionZero{1, 1};

    mover.arrive(positionZero);

    Position position1{2,2};
    mover.moveTo(position1);

    BoardNote boardNote0{4, 0};
    BoardNote boardNote1{1, 0}
    BoardNote boardNote2{2, 0}
    BoardNote boardNote3{3, 0}
    BoardNote boardNote4{4, 0};

    expectedNotes.insert(boardNote0);
    expectedNotes.insert(boardNote1);
    expectedNotes.insert(boardNote2);
    expectedNotes.insert(boardNote3);
    expectedNotes.insert(boardNote4);

    BoxNote boxNote0{11, positionZero, positionZero, std::chrono_high_resolution_clock::now()};
    BoxNote boxNote1{10, position1, positionZero, std::chrono_high_resolution_clock::now()}; 
    BoxNote boxNote2{11, position1, positionZero, std::chrono_high_resolution_clock::now()};
}*/
